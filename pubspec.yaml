name: bwthw_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  http: 1.2.1
  jwt_decoder: ^2.0.1
  fl_chart : 1.2.0
  intl: ^0.20.2
  provider: ^6.1.5+1
  shared_preferences:
  crypto: ^3.0.7
  flutter_dotenv: ^6.0.1
  json_schema: ^5.2.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
  assets:
    - assets/.env
